description "LZMA compressor (slow, but very good ratio)\3770,128,128";

uses
	Core;

library
	lzma;

external_dependency(WIN32) liblzma;

file
	lzma.h,
	lzma.cpp,
	src.tpp,
	lib readonly separator,
	Copying;

